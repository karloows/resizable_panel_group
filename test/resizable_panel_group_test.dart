import 'package:flutter_test/flutter_test.dart';
import 'package:resizable_panel_group/resizable_panel_group.dart';

void main() {
  test('exports the package entrypoint', () {
    expect(packageName, 'resizable_panel_group');
  });
}
