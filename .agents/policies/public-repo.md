# Public repo policy

This repo is public on GitHub. Applies to every commit, branch, PR, and issue:

- Never commit secrets, tokens, API keys, or personal data.
- Never include real names or emails in code comments or commit messages beyond
  standard git authorship.
- Treat everything written here — code comments, README text, commits, PRs —
  as something any stranger can read today.
